<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>To Do List</title>
  </head>
  <body>

    <% if (kindOfDay === "Tuesday" || kindOfDay === "Sunday") { %>
      <h1 style="color: red"><%= kindOfDay %> List</h1>
    <% } else { %>

    <h1 style="color: blue"><%= kindOfDay %> List</h1>
    <%  } %>

  </body>
</html>
